welcome.message=Welcome to our application 

#entities
system.currentVersion.notnull = currentVersion is must not null
system.currentVersion.size = currentVersion size min 3 and max 10 

system.supportsVersions.notnull = supportsVersions is must not null
system.supportsVersions.size = supportsVersions size min 1 and max 100 

system.creator.notnull = creator is must not null
system.creator.size = creator size min 1 and max 30 

category.name.notnull = name is must not null
category.name.size = name size min 1 and max 30 

category.description.notnull = description is must not null
category.description.size = description size min 1 and max 30 

category.enabled.notnull = enabled is must not null
category.enabled.size = enabled size min 1 and max 30 

link.urlText.notnull = urlText is must not null
link.urlText.size = urlText size min 1 and max 500 

link.description.notnull = description is must not null
link.description.size = description size min 1 and max 500 

link.fkCategory.notnull = fkCategory is must not null

link.enabled.notnull = enabled is must not null
link.enabled.size = enabled size min 1 and max 30 


#end entities